import * as React from 'react';

import Typography from 'material-ui/Typography';

interface Props { };

export const NotFoundLayout: React.SFC<Props> = (props) => (
  <Typography type="title">404 Route Not Found</Typography>
);
